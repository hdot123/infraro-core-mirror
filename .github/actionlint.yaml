self-hosted-runner:
  # 自建 runner label，actionlint 内置清单不认识，需显式声明：
  # - pve-linux: org 级 Linux runner（[REDACTED-HOST] / [REDACTED-HOST]，全部 CI job）
  # - memory-core-upgrade: org 级 Mac runner（memory-core-mac，发版 upgrade-consumer 专属）
  labels:
    - pve-linux
    - memory-core-upgrade

paths:
  .github/workflows/evolution-scan.yml:
    ignore:
      # rhysd/actionlint#647（跟踪：memory/inbox.md 记债条目；rhysd/actionlint#647 上游修复后删除本条目）
      - 'property "workflow_sha" is not defined in object type'
  .github/workflows/evolution-heartbeat.yml:
    ignore:
      # rhysd/actionlint#647（跟踪：memory/inbox.md 记债条目；rhysd/actionlint#647 上游修复后删除本条目）
      - 'property "workflow_sha" is not defined in object type'
  .github/workflows/droid-review-shards.yml:
    ignore:
      # rhysd/actionlint#647（跟踪：memory/inbox.md 记债条目；rhysd/actionlint#647 上游修复后删除本条目）
      - 'property "workflow_sha" is not defined in object type'
